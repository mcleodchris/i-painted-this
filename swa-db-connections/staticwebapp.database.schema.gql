"""
Add your CosmosDB NoSQL database schema in this file

For example:

type Book @model {
  id: ID
  title: String
}
"""
type Entry @model {
    id: ID!
    item: String!
    game: String!
    modelCount: String!
    completedDate: DateTime!
    createdAt: DateTime!
}
